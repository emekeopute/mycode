#!/usr/bin/env python3

count = 0
hallway = ["toy", "frame", "toy"]

for toy in hallway:
    if toy == "toy"
        count =+ 1
        hallway.remove(count)
        print(f"I found a {toy} in the hallway and removed it")
        print(hallway)
